example
example
	"self example"
	"Inspect the expression to inspect the resulting memo dictionary."
	"Note that computation is actually much slower than simple 
	Fibonacci
	because it does not avoid most of recomputations (results 
	arrive too late!).
	See SmartMemoFunction as a less naive version."
	^ self new active n: 10 replyTo: Print